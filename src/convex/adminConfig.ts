/**
 * Admin panel credentials — these live only in the Convex backend, never in
 * the browser bundle. For production you can move them to environment
 * variables (Keys/API keys UI) and read them with process.env.
 */
export const ADMIN_USERNAME = "GolamRabbi786";
export const ADMIN_PASSWORD = "G.rabbi3046";
